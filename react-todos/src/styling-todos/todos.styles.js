/**
 * Inline styles
 *
 * PROS:
 * - dinamici
 * - applicabili localmente
 * - riusabili
 *
 * CONS:
 * - nessuna evidenziazione sintassi CSS
 * - non applicabili globalmente
 * - niente regole globali (media queries)
 * - ridondati su ogni elemento
 */

const styles = {
  done: {
    textDecoration: "line-through",
  },
};

export default styles;
